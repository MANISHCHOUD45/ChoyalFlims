# Where the site loads its content from at runtime.
# Set to a public, CORS-enabled URL that returns the content JSON — see
# public/content.json for the expected shape. Leave blank to use the bundled
# public/content.json. Content behind this URL is live: edit it and refresh —
# no rebuild needed.
VITE_CONTENT_URL=
